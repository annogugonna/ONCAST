---
version: 2
plan:
  key: OPS
  name: Your Plan Name
  project-key: ON
  stage:
    - default:
        jobs:
          - job:
              key: JOB2
              name: Echo Message
              script:
                - echo "Hello, this is an echo message."

