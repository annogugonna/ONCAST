---
version: 2

plan:
  key: OPS
  name: Your Plan Name
  project-key: ON
  stage:
    - default:
        jobs:
          - job:
              key: JOB1
              name: Echo Message
              script:
                - echo "BUILD BUILD BUILD."

